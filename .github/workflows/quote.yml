name: Update Coding Quote

on:
  schedule:
    - cron: '0 3 * * *'  # Roz 3 AM UTC = 8:30 AM IST
  workflow_dispatch:

jobs:
  update-quote:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install requests

      - name: Run quote updater
        run: python quote.py

      - name: Commit and push
        run: |
          git config user.name "ankitaa2603"
          git config user.email "ankitagupta2603@gmail.com"
          git add README.md
          git diff --staged --quiet || git commit -m "✨ Daily quote updated"
          git push
